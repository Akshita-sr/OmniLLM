# Core dependencies
litellm>=1.40.0
rich>=13.0
click>=8.1
pyyaml>=6.0
aiohttp>=3.9
python-dotenv>=1.0

# Development / testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12

# Robotics (optional)
websockets>=12.0
flask>=3.0

# HRI / Embodied LLM Arena (optional — install with pip install "omnillm[hri]")
# chromadb>=0.4.0
# langchain-community>=0.2.0
# sentence-transformers>=3.0
# pypdf>=4.0
# langdetect>=1.0.9
# langgraph>=0.2.0
# openai-whisper  (for local STT)
# openai>=1.0     (for Whisper API STT)

